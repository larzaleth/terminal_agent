// Plain React createElement alias — we invoke it in createElement form
// throughout the UI (h(Component, props, ...children)), so no template
// literal binding needed.
export { createElement as h } from "react";
